# EXPERIMENTS

None running.

Cheapest tests that would change strategy (not started):

1. Live 20-min 3-bill session with one ICP founder. Distinguishes “product is the bottleneck” vs “nobody has seen it”.
2. Disposable workspace: paste 3 distinct single-vendor bills → POST `/api/workspaces/current/activation`. Expected today: `deferred-no-picture`. Distinguishes C2.
3. Resolve A9 Reddit URL or drop the row. Distinguishes “signal bank is clean” vs “one fabricated thread”.
4. After Keep on a price-increase merchant, screenshot Coming later amount vs the decision-card amount. Distinguishes landing “not an estimate” vs product.
