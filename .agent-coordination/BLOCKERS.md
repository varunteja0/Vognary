# BLOCKERS

## HUMAN — do not fake

- Confirm F03–F06 LinkedIn sent vs unsent. CRM `asked_at` stays blank until founder says the time + channel.
- Public reply on Prashanth Fable-limits post. Do not resend the 2026-08-20 linger thread.
- Gmail: confirm forwarding on the v1 alias, then billing-only filter. Do not send a receipt before confirm. `forwarding_verified_at` is still null.
- Google Search Console removal of the indexed `/app?demo=1` URL.
- Live 3-bill session (Prashanth if he engages; otherwise any F-row that replies).

## EVIDENCE — do not ship around

- First automatic receipt, second automatic receipt, provider replay: NOT PROVEN.
- Reminder delivery: schedule exists; Resend send fail-closed.
- `workspace.activated` as first-10 activation: measurement mismatch, not a deploy blocker.

## AGENT

- Ox dirty tree: Coming later / cookie / excerpt. Grok did not edit those files.
- Do not send Autopilot-cancel copy (Phase A §3 and historical block of the gitignored draft).
